let SessionLoad = 1
let s:so_save = &g:so | let s:siso_save = &g:siso | setg so=0 siso=0 | setl so=-1 siso=-1
let v:this_session=expand("<sfile>:p")
silent only
silent tabonly
cd ~/.config/nvim
if expand('%') == '' && !&modified && line('$') <= 1 && getline(1) == ''
  let s:wipebuf = bufnr('%')
endif
let s:shortmess_save = &shortmess
if &shortmess =~ 'A'
  set shortmess=aoOA
else
  set shortmess=aoO
endif
badd +72 lua/core/plugins.lua
badd +1 lua/core/theme.lua
badd +47 ~/storm-monitor/main.go
badd +1 ~/storm-monitor/nvim/lua/core/theme.lua
badd +1 ~/storm-monitor/nvim/lua/core/plugins.lua
badd +102 ~/storm-monitor/monitor/storm.go
badd +834 ~/go/pkg/mod/git.code.oa.com/snowzhu/teejson@v1.0.6/teejson.go
badd +12 lua/configs/filetree.lua
badd +12 init.vim
badd +11 ~/git/studynote/Home.html
badd +153 ~/go/pkg/mod/git.code.oa.com/teego/teego@v1.0.62-pre1/app.go
badd +112 lua/core/keymaps.lua
badd +30 lua/core/init.lua
badd +1 lua/configs/vim.lua
badd +11 lua/configs/treesitter.lua
badd +14 lua/configs/lspconfig.lua
badd +30 ~/storm-monitor/router/base.go
badd +10 ~/storm-monitor/router/routes.go
badd +102 ~/go/pkg/mod/github.com/gin-gonic/gin@v1.4.0/routergroup.go
argglobal
%argdel
edit init.vim
let s:save_splitbelow = &splitbelow
let s:save_splitright = &splitright
set splitbelow splitright
let &splitbelow = s:save_splitbelow
let &splitright = s:save_splitright
wincmd t
let s:save_winminheight = &winminheight
let s:save_winminwidth = &winminwidth
set winminheight=0
set winheight=1
set winminwidth=0
set winwidth=1
argglobal
let s:l = 12 - ((11 * winheight(0) + 34) / 69)
if s:l < 1 | let s:l = 1 | endif
keepjumps exe s:l
normal! zt
keepjumps 12
normal! 0
tabnext 1
if exists('s:wipebuf') && len(win_findbuf(s:wipebuf)) == 0 && getbufvar(s:wipebuf, '&buftype') isnot# 'terminal'
  silent exe 'bwipe ' . s:wipebuf
endif
unlet! s:wipebuf
set winheight=1 winwidth=20
let &shortmess = s:shortmess_save
let &winminheight = s:save_winminheight
let &winminwidth = s:save_winminwidth
let s:sx = expand("<sfile>:p:r")."x.vim"
if filereadable(s:sx)
  exe "source " . fnameescape(s:sx)
endif
let &g:so = s:so_save | let &g:siso = s:siso_save
set hlsearch
doautoall SessionLoadPost
unlet SessionLoad
" vim: set ft=vim :
